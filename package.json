{
  "private": true,
  "scripts": {
    "test": "jest",
    "inst": "set NODE_TLS_REJECT_UNAUTHORIZED=0&&npm install"
  },
  "devDependencies": {
    "chalk": "^2.4.1",
    "jest": "^23.5.0",
    "puppeteer": "^1.7.0",
    "rimraf": "^2.6.2"
  }
}
